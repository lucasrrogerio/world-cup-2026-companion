## 1. Localization

- [ ] 1.1 Add filter labels to all locale files (`album.filter_all`, `album.filter_missing`, `album.filter_duplicates`).

## 2. UI Implementation

- [ ] 2.1 Add filter state and UI controls in `Album.jsx`.
- [ ] 2.2 Update `StickerGrid.jsx` to receive and apply the `filterMode`.
- [ ] 2.3 Implement the filtering logic in `StickerGrid.jsx` (filter stickers then group them).

## 3. Empty States & Animation

- [ ] 3.1 Add an "Empty State" component for when a filter yields no results.
- [ ] 3.2 Ensure smooth transitions using Framer Motion when filter changes.
