## 1. Guest Data Privacy

- [x] 1.1 Update `useCollection.js` to clear `cityDuplicates` when `user` is null.
- [x] 1.2 Add a conditional check in `StickerGrid.jsx` to hide the `RefreshCw` icon (swap indicator) for guests.

## 2. Theme Refinement

- [x] 2.1 Update `index.css` with new Light Mode variables (Slate 50 background, improved gold contrast).
- [x] 2.2 Adjust `ProgressChart.jsx` to use theme-aware colors for labels and axes.
- [x] 2.3 Verify button and card shadows in Light Mode for a more "premium" lifted look.
