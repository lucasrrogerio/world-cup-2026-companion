## 1. Style Integration

- [x] 1.1 Add `--card-owned-bg` to `:root` (dark) and `[data-theme="light"]` in `src/index.css`.
- [x] 1.2 Update `StickerCard.jsx` to dynamically switch between `--card-bg` and `--card-owned-bg`.

## 2. Verification

- [x] 2.1 Verify that owned stickers look "lighter" and more vibrant in Dark Mode.
- [x] 2.2 Ensure no regression in Light Mode.
